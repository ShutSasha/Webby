apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  type: ClusterIP
  ports:
    - name: rest
      port: 5006
      targetPort: 5006
    - name: grpc
      port: 5007
      targetPort: 5007
