apiVersion: v1
kind: Service
metadata:
  name: room-category-service
spec:
  selector:
    app: room-category-service
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: 8082
