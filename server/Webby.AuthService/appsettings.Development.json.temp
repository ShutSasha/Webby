{
   "Logging": {
      "LogLevel": {
         "Default": "Information",
         "Microsoft.AspNetCore": "Warning"
      }
   },
   "ConnectionStrings": {
      "AppDbContext": "{{DatabaseConnectionString}}"
   },
   "SenderData": {
      "SenderPassword": "{{SenderPassword}}",
      "SenderEmail" : "{{SenderEmail}}"
   },
   "JwtOptions": {
      "AccessSecretKey": "{{JwtAccessSecretKey}}",
      "AccessExpiresDuration": "{{JwtAccessExpiresDuration}}"
   }
}
